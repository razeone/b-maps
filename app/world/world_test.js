'use strict';

describe('bMaps.world module', function() {

  beforeEach(module('bMaps.world'));

  describe('world controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var worldCtrl = $controller('WorldController');
      expect(worldCtrl).toBeDefined();
    }));

  });
});